{
  "name": "roll20-mute",
  "scripts": {
    "build": "mkdir build && cp -r public manifest.json service-worker.js build && crx pack build"
  },
  "devDependencies": {
    "chrome-types": "^0.1.227",
    "crx": "^5.0.1"
  }
}
